node_modules/
# Local pi state (e.g. project-scoped `pi install -l` writes here) — never commit
.pi/
*.log
.DS_Store
.env
# Zotero DB backups the script may create if run from inside the repo
*.pi-backup-*
zotero.sqlite*
